


# 弥厶釉记账 — 产品文档与技术规范


> **文档版本**：v1.0
> **创建日期**：2026-08-11
> **最后更新**：2026-08-11

---

## 一、项目概述

| 项目 | 说明 |
|------|------|
| **项目名称** | 弥厶釉记账 |
| **项目定位** | 简单易用的个人记账桌面应用 |
| **目标用户** | 个人使用，非商业 |
| **运行平台** | Windows 10/11 桌面 |
| **语言** | 界面文字和代码注释统一使用简体中文 |
| **货币** | 人民币（CNY），符号 ¥，单位 元 |

---

## 二、技术栈（已选定）

| 层级 | 技术选型 | 选择原因 |
|------|---------|----------|
| **运行方式** | 纯网页应用 | 浏览器打开即可使用，也可通过 Vite 开发服务器运行 |
| **前端界面** | React + TypeScript | 界面美观现代，组件化开发，代码可维护 |
| **界面样式** | CSS（全局样式变量） | 简洁统一的样式系统，支持亮色主题 |
| **数据存储** | IndexedDB（浏览器本地数据库） | 数据存储在浏览器中，关闭后不丢失，无需安装任何软件 |
| **构建工具** | Vite | 快速的前端构建工具 |

---

## 三、核心功能清单

### 第一期：基础功能（当前开发目标）

| 序号 | 功能 | 描述 | 状态 |
|------|------|------|------|
| 1 | 记录支出 | 输入金额、选择分类、填写备注、选择日期 | ⬜ 待开发 |
| 2 | 查看账单列表 | 按时间倒序显示所有记录，支持按月筛选 | ⬜ 待开发 |
| 3 | 分类统计 | 饼图/柱状图展示本月各类支出占比和金额 | ⬜ 待开发 |
| 4 | 修改/删除记录 | 对已有记录进行编辑或删除 | ⬜ 待开发 |
| 5 | 数据持久化 | SQLite 本地存储，关闭应用后数据不丢失 | ⬜ 待开发 |

### 第二期：增强功能（后续迭代）

| 序号 | 功能 | 描述 |
|------|------|------|
| 6 | 按月切换查看 | 查看历史月份的账单记录 |
| 7 | 月度预算设置 | 设定月度预算，超支时显示提醒 |
| 8 | 搜索记录 | 按备注关键词搜索历史记录 |
| 9 | 自定义分类 | 用户可以自行增加、修改、删除分类 |

### 第三期：扩展功能（远期规划）

| 序号 | 功能 | 描述 |
|------|------|------|
| 10 | 数据导出 | 导出为 Excel/CSV 文件 |
| 11 | 收入记录 | 不只记支出，也记录收入 |
| 12 | 多账户管理 | 现金、银行卡、微信、支付宝等账户 |
| 13 | 数据备份恢复 | 手动备份数据库文件，支持恢复 |

---

## 四、分类体系

### 设计说明
- 分类为**两级结构**：一级大类 → 二级小类
- 用户记账时先选一级大类，再选二级小类
- 以下为预设默认分类，用户可在第二期功能中自定义修改

### 完整分类表（10 个一级大类，50 个二级小类）

```
🍽️ 餐饮美食
   ├── 早餐
   ├── 午餐
   ├── 晚餐
   ├── 零食饮料
   ├── 外卖
   ├── 聚餐聚会
   ├── 水果
   └── 买菜食材

🚗 交通出行
   ├── 公交地铁
   ├── 打车
   ├── 加油充电
   ├── 停车费
   ├── 火车高铁
   ├── 飞机票
   ├── 共享单车
   └── 汽车保养维修

🛒 购物消费
   ├── 衣服鞋帽
   ├── 日用百货
   ├── 数码电子
   ├── 化妆品护肤
   ├── 家居用品
   ├── 宠物用品
   └── 网购其他

🏠 居住生活
   ├── 房租房贷
   ├── 水电燃气
   ├── 物业费
   ├── 网费话费
   ├── 维修
   └── 家居装修

🎮 娱乐休闲
   ├── 电影演出
   ├── 游戏
   ├── 旅游度假
   ├── 运动健身
   ├── KTV酒吧
   ├── 书籍阅读
   ├── 视频会员
   └── 咖啡茶馆

💊 医疗健康
   ├── 看病挂号
   ├── 药品
   ├── 体检
   ├── 牙科眼科
   ├── 保健品
   └── 理发造型

📚 教育学习
   ├── 培训课程
   ├── 书籍资料
   ├── 考试报名
   ├── 文具
   └── 在线会员

🎁 人情往来
   ├── 红包礼金
   ├── 请客送礼
   ├── 孝敬父母
   ├── 慈善捐款
   └── 礼物赠送

💰 金融理财
   ├── 保险
   ├── 投资亏损
   ├── 手续费
   └── 贷款利息

📦 其他支出
   ├── 快递邮寄
   └── 其他杂项
```

---

## 五、数据模型

### 支出记录表（expenses）

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `id` | INTEGER | 自增主键 | 1 |
| `amount` | REAL | 金额（元），保留两位小数 | 35.50 |
| `category1` | TEXT | 一级分类名称 | "餐饮美食" |
| `category2` | TEXT | 二级分类名称 | "外卖" |
| `date` | TEXT | 消费日期 | "2026-08-11" |
| `note` | TEXT | 备注（可选） | "黄焖鸡米饭" |
| `created_at` | TEXT | 记录创建时间戳 | "2026-08-11T20:30:00" |

### 分类表（categories）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | INTEGER | 自增主键 |
| `name` | TEXT | 分类名称 |
| `icon` | TEXT | 图标（emoji） |
| `parent_id` | INTEGER | 父分类 ID（NULL 表示一级分类） |
| `sort_order` | INTEGER | 排序序号 |

---

## 六、项目文件结构（规划）

```
记账APP/
├── CLAUDE.md                    ← 本文件（项目说明 + AI 行为规范）
├── package.json                 ← Node.js 项目配置
├── tsconfig.json                ← TypeScript 配置
├── vite.config.ts               ← Vite 构建配置
├── electron-builder.yml         ← 打包配置
│
├── electron/                    ← Electron 主进程代码
│   ├── main.ts                  ← 主进程入口（窗口管理、生命周期）
│   ├── preload.ts               ← 预加载脚本（安全桥接）
│   └── database.ts              ← SQLite 数据库操作
│
├── src/                         ← React 前端代码（渲染进程）
│   ├── main.tsx                 ← React 入口
│   ├── App.tsx                  ← 应用根组件
│   ├── components/              ← 可复用组件
│   │   ├── Layout.tsx           ← 页面布局框架
│   │   ├── ExpenseForm.tsx      ← 记账表单
│   │   ├── ExpenseList.tsx      ← 账单列表
│   │   ├── CategoryPicker.tsx   ← 分类选择器
│   │   └── StatsChart.tsx       ← 统计图表
│   ├── pages/                   ← 页面
│   │   ├── HomePage.tsx         ← 首页（今日概览）
│   │   ├── RecordPage.tsx       ← 记账页面
│   │   ├── HistoryPage.tsx      ← 历史账单页面
│   │   └── StatsPage.tsx        ← 统计页面
│   ├── store/                   ← 数据状态管理
│   │   └── expenseStore.ts      ← 支出数据状态
│   ├── utils/                   ← 工具函数
│   │   ├── format.ts            ← 金额/日期格式化
│   │   └── categories.ts        ← 默认分类数据
│   └── styles/                  ← 样式文件
│       └── global.css           ← 全局样式
│
├── assets/                      ← 静态资源
│   └── icon.png                 ← 应用图标
│
└── data/                        ← 默认数据
    └── default-categories.json  ← 预设分类数据
```

---

## 七、🚨 重要：开发行为规范（AI 助手必须遵守）

### 用户背景说明
本项目的用户是**不懂编程技术的小白**，首次使用 Claude Code。

### 核心规则（必须遵守）

> **规则 1：任何技术方面的决策，AI 不能自行决定。**
>
> 遇到需要做技术选择时（如：选什么库、用什么方式实现、UI 怎么布局、颜色方案等），AI 必须：
> 1. 列出 2-4 个可行方案
> 2. 用通俗易懂的大白话解释每个方案是什么
> 3. 列出每个方案的优缺点
> 4. 给出一个推荐方案并说明理由
> 5. 等待用户做出选择后再继续

> **规则 2：解释技术概念时必须通俗。**
>
> 避免使用专业术语。如果必须使用，需要附带解释。把用户当成完全不懂编程的朋友来沟通。

> **规则 3：每次操作前做简要说明。**
>
> 在写代码或执行命令之前，用简单的话告诉用户准备做什么、为什么这样做。不要直接闷头写代码。

> **规则 4：每次修改后做总结。**
>
> 代码修改完成后，用简洁的话总结改了哪些文件、实现了什么功能。

> **规则 5：不确定的事情先问。**
>
> 对用户需求有任何不确定的地方，先提问确认，不要自己猜测假设。

> **规则 6：金额相关必须精确。**
>
> 涉及金额显示、计算、存储的代码，必须精确到两位小数（分为单位），不能有浮点数精度问题。建议金额在内部以"分"为单位存储（整数），显示时除以 100 转为元。

---

## 八、设计原则

1. **简洁优先** — 界面不要花里胡哨，功能一目了然
2. **操作直观** — 记账流程不超过 3 步：输入金额 → 选分类 → 保存
3. **本地运行** — 不需要网络，不连接任何服务器
4. **数据安全** — 所有数据只存储在用户本地电脑
5. **响应式设计** — 窗口可以调整大小，界面自适应

---

## 九、决策记录

| 日期 | 决策内容 | 可选方案 | 最终选择 | 选择原因 |
|------|---------|---------|---------|---------|
| 2026-08-11 | 技术框架 | A:纯网页 / B:Electron / C:Tauri / D:Python | **B: Electron** | 用户选择，希望有真正的桌面软件体验 |
| 2026-08-11 | 前端框架 | React + TypeScript | **React + TypeScript** | 生态成熟，社区资源丰富，适合 Electron |
| 2026-08-11 | 数据存储 | SQLite / JSON文件 / localStorage | **SQLite** | 结构化查询、数据安全、适合桌面应用 |
| 2026-08-11 | SQLite 库选择 | better-sqlite3 / sql.js / JSON文件 | **sql.js** | better-sqlite3 需要 VS C++ 编译器，sql.js 纯 JS 无需编译，功能相同 |
| 2026-08-12 | 运行方式调整 | Electron 桌面应用 / 纯网页应用 | **纯网页应用** | Electron 二进制在 Windows 上存在模块解析问题，切换为纯网页方案，使用 IndexedDB 存储 |

---

## 十、给用户的快速指引

### 你现在需要知道的事情：

1. **项目所在文件夹**：`e:\entertainment\记账APP\`
2. **技术方案已选定**：Electron（真正的 Windows 桌面软件）
3. **下一步**：AI 会帮你一步一步搭建开发环境和创建项目
4. **运行方式**：开发完成后，你会得到一个 .exe 安装程序，安装后就能像普通软件一样使用
5. **遇到任何不懂的地方**：直接问 AI "这是什么意思"，AI 会用大白话解释
